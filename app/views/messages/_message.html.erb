<div id="message-<%= message.sender.id %>">
  <small>
    <strong><%= message.sender %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
