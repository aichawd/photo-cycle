<div class="container">
  <h1>New photo</h1>
  <%= simple_form_for(@picture) do |f| %>
    <%= f.input :photo, as: :file %>
    <%= f.input :caption %>
    <%= f.submit "Add to my gallery", class: "btn btn-secondary float-right" %>
  <% end %>
</div>
