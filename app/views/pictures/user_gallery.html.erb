<head>
  <link rel="stylesheet" href="user_gallery.scss">
</head>

<div class='user-banner'>
    <h1><%= @user.first_name %> <%= @user.last_name %></h1>
    <%# <p><%= @user.username %>
    <ol>
      <li><%= @user_pictures.count %> masterpieces</li>
      <% if @user == current_user %>
        <li>
          <button type="button" class="btn btn-secondary" data-bs-toggle="modal" data-bs-target="#newPhotoModal">
            Add photo
          </button>
        </li>
      <% else %>
        <li>
          <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#collabModal">
            Collaborate
          </button>
        </li>
      <% end %>

      <li>Matériel: Kidizoom 🥲</li>
    </ol>
    <p>Bio: artichette, tichette de 2, tichette de 3, tichette de 21, michette, tchiledeque, mique,sganabadarlane, résiné et raitournelle..</p>
</div>

<%= render 'modals' %>

<hr>

<div class="photo-behavior">
  <ul>
    <% @user_pictures.each do |pic| %>
      <li><%= cl_image_tag pic.photo.key %></li>
    <% end %>
  </ul>
</div>
